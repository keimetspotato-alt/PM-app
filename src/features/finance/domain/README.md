# 計算ロジック

将来の収支計算はこのフォルダに配置します。Reactやブラウザに依存しない純粋な関数として実装してください。

- 円の金額は整数で扱い、端数の丸め方を明示する。
- 日付や計算条件を引数で受け取り、同じ入力には同じ結果を返す。
- 将来予測の前提（収入、支出、期間、物価変動など）を明示する。
- 実装時に隣接する `*.test.ts` へ境界値と代表例のテストを追加する。
